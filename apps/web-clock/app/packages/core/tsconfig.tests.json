{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Tests may use the vitest runtime; src stays platform-free via tsconfig.json.
    "lib": ["ES2022"],
    "types": ["vitest/globals"]
  },
  "include": ["src", "tests"]
}
