# Task — local rebase prep for Pace #83, #80, #78

**Dispatched:** 2026-09-14 11:00 (standup cutoff, no founder reply — 08:00 proposal item 2 proceeds)
**Profile:** app-engineer (`agents/app-engineer.md`)
**Reviewer:** tech-lead → founder (founder merges; nothing is pushed to his remote)

## Objective
Rebase the three conflicting non-draft running-with-pace PRs onto current `main`
(`913a257`) in **local worktrees**, resolve conflicts, and hand the founder reviewable
diffs — so each PR becomes a one-command push when he says go.

## Scope
- `#83` Preserve profile avatars across profile saves (8d, conflicting)
- `#80` Collectible goal badges and journal gallery (10d, conflicting)
- `#78` Bring the ASO visual system into Pace (10d, conflicting)

Precedent: `#88`'s finished rebase is parked at `4cd874b` in
`~/src/running-with-pace/.worktrees/pr88-rebase`, unpushed. Match that pattern —
one worktree per PR under `.worktrees/pr<N>-rebase`.

## Definition of done
- **Output file (required): `apps/running-with-pace/notes/2026-09-14-rebase-prep.md`**
  in `~/src/app-factory`, committed on `main` and pushed. Per PR it records: the
  worktree path, the resulting head SHA, every conflict hunk and how it was resolved,
  the check/test result, and a verdict — **ready to push** / **needs the author** /
  **should be closed instead**.
- Each rebased branch exists locally at a named head SHA with a clean `git status`.
- Rig checks run per rebased branch where feasible (`npm run type-check`,
  `npm run lint`, `cd pace-node-js-server && npm run build`, `npm run test:unit`);
  record pass/fail verbatim, do not paper over a failure.
- If a rebase is not honestly resolvable — conflicting intent, not conflicting text —
  stop on that one, say so in the note, and move to the next. A wrong resolution is
  worse than an unrebased branch.

## Boundaries — hard
- **No force-push, no push of any kind to `origin`.** No PR comments, no merges,
  no closes, no branch deletion on the remote.
- Do not touch Pace `main`'s existing dirty working tree (27 paths, mtime 09-09) —
  that is a separate item under founder review. Work only in fresh worktrees.
- Do not delete or prune any existing worktree.

## Report
Post outcome-first in Slack `#factory-pace` (C0BPEDXGG00): per-PR verdict, head SHAs,
the note's commit SHA. Explicitly state that nothing was pushed.
