# Dormant profiles

Charters ready to activate. Per D11, activating one requires a decision-log entry naming
the bottleneck it solves; until then their duties fall to the nearest active role
(noted per profile). Move the file up to `agents/` and set `status: active` to activate.
