{
  "name": "@factory/analytics",
  "version": "0.1.0",
  "private": true,
  "description": "Typed analytics seam. Features emit typed events; provider adapters live in the app shell. Raw provider calls outside the adapter are forbidden.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.tests.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "vitest": "^3.2.4"
  }
}
