// Design system barrel. Components import { color, space, radius, type } from here —
// never raw hex/px, never the generated file directly.
//
// Primitives (card.tsx, screen.tsx, glow-text.tsx, ...) are added per app and
// exported here by name.

export { color, lightColor, modes, space, radius, type } from './tokens.generated.js';
